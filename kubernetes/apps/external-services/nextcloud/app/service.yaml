---
apiVersion: v1
kind: Service
metadata:
  name: ${APP}
  labels:
    app: ${APP}
spec:
  type: ClusterIP
  clusterIP: None  # Headless service for external endpoints
  ports:
    - name: https
      port: 443
      protocol: TCP
      targetPort: 443
