---
# Remove the default PodSecurity admission config (was a JSON6902 remove, which
# talhelper no longer supports with multi-document machine configs)
cluster:
  apiServer:
    admissionControl:
      - name: PodSecurity
        $$patch: delete
