# yaml-language-server: $schema=https://raw.githubusercontent.com/budimanjojo/talhelper/master/pkg/config/schemas/talconfig.json
---
clusterName: kubernetes

talosVersion: "${talosVersion}"
kubernetesVersion: "${kubernetesVersion}"

endpoint: https://192.168.1.6:6443
additionalApiServerCertSans: &sans
  - "127.0.0.1"
  - "192.168.1.6"
  - "cluster.local"
  - "cluster.tostava.com"
additionalMachineCertSans: *sans

clusterPodNets: ["10.42.0.0/16"]
clusterSvcNets: ["10.43.0.0/16"]

# Disable built-in CNI to use Cilium
cniConfig:
  name: none

nodes:
  - hostname: "k3s"
    ipAddress: "192.168.1.5"
    installDiskSelector:
      serial: "naa.5002538e01126c0e"
    machineSpec:
      secureboot: true
    talosImageURL: factory.talos.dev/installer-secureboot/c1df0cd7d7aaf563d0b867cafb0ab994e1bf51bb8ce11a0612b0a0bd502a2ce7
    controlPlane: true
    networkInterfaces:
      - deviceSelector:
          hardwareAddr: "94:40:c9:1e:82:c0"
        dhcp: false
        addresses:
          - "192.168.1.5/24"
        routes:
          - network: "0.0.0.0/0"
            gateway: "192.168.1.1"
        mtu: 1500
        vip:
          ip: "192.168.1.6"

# Global patches
patches:
  - "@./patches/global/machine-files.yaml"
  - "@./patches/global/machine-kubelet.yaml"
  - "@./patches/global/machine-network.yaml"
  - "@./patches/global/machine-sysctls.yaml"
  - "@./patches/global/machine-time.yaml"

# Controller patches
controlPlane:
  patches:
    - "@./patches/controller/admission-controller-patch.yaml"
    - "@./patches/controller/cluster.yaml"

